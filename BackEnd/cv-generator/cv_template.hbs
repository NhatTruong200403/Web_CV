<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CV - {{personalInfo.name}}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700&display=swap" rel="stylesheet">
    <style>
        /* --- Reset & Base Styles --- */
        :root {
            /* Color Palette */
            --primary-color: #2a628f; /* Xanh dương đậm hơn, chuyên nghiệp */
            --secondary-color: #4e8d7c; /* Xanh lá cây trầm làm điểm nhấn */
            --text-color: #333a40; /* Màu chữ chính */
            --text-muted: #6c757d; /* Màu chữ phụ, xám */
            --background-light: #f8f9fa; /* Nền sáng */
            --background-white: #ffffff;
            --border-color: #dee2e6; /* Màu viền */
            --tag-bg: #eaf2f8; /* Nền tag */
            --tag-text: var(--primary-color);
            --tag-border: #c5d9e8;

            /* Typography */
            --font-primary: 'Inter', sans-serif;
            --font-headings: 'Poppins', sans-serif;

            /* Spacing (multiples of 4px/8px) */
            --space-xs: 0.25rem; /* 4px */
            --space-sm: 0.5rem;  /* 8px */
            --space-md: 1rem;    /* 16px */
            --space-lg: 1.5rem;  /* 24px */
            --space-xl: 2rem;    /* 32px */
            --space-xxl: 2.5rem; /* 40px */

            /* Other */
            --border-radius: 6px;
            --transition-speed: 0.3s;
            --box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }

        *, *::before, *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html {
            font-size: 16px; /* Base font size */
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-primary);
            line-height: 1.7;
            background-color: var(--background-light);
            color: var(--text-color);
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            padding: var(--space-lg) 0; /* Add padding top/bottom */
        }

        /* --- Container --- */
        .container {
            max-width: 900px; /* Slightly wider */
            margin: 0 auto; /* Center container */
            background: var(--background-white);
            padding: var(--space-xxl);
            box-shadow: var(--box-shadow);
            border-radius: var(--border-radius);
            border-top: 5px solid var(--primary-color); /* Top accent border */
        }

        /* --- Typography --- */
        h1, h2, h3, h4, h5, h6 {
            font-family: var(--font-headings);
            font-weight: 600; /* Semi-bold */
            color: var(--primary-color);
            margin-bottom: var(--space-sm);
            line-height: 1.3;
        }

        h1 { /* Tên ứng viên */
            font-size: 2.8rem; /* Larger size */
            font-weight: 700; /* Bold */
            text-align: center;
            margin-bottom: var(--space-sm);
            letter-spacing: 0.5px;
        }

        h2 { /* Tiêu đề Section */
            font-size: 1.7rem;
            font-weight: 600;
            color: var(--primary-color);
            border-bottom: 2px solid var(--border-color);
            padding-bottom: var(--space-sm);
            margin-top: var(--space-xl);
            margin-bottom: var(--space-lg);
        }

        p {
            margin-bottom: var(--space-md);
        }
        p:last-child {
            margin-bottom: 0; /* Remove margin from last paragraph in a block */
        }

        a {
            color: var(--secondary-color); /* Use secondary color for links */
            text-decoration: none;
            font-weight: 500; /* Medium weight for links */
            transition: color var(--transition-speed) ease, text-decoration var(--transition-speed) ease;
        }

        a:hover {
            color: var(--primary-color);
            text-decoration: underline;
            text-decoration-thickness: 1.5px; /* Nicer underline */
            text-underline-offset: 3px; /* Space out underline */
        }

        ul {
            list-style: none; /* Remove default list styles */
            padding: 0;
            margin: 0;
        }

        /* --- Header Sections --- */
        .personal-info, .contact-links {
            text-align: center;
            margin-bottom: var(--space-lg);
            padding-bottom: var(--space-lg);
            border-bottom: 1px solid var(--border-color);
        }
        .contact-links {
             border-bottom: none; /* Remove double border */
             padding-bottom: 0;
        }

        .personal-info p {
            font-size: 1.05rem;
            color: var(--text-muted);
            margin-bottom: var(--space-xs); /* Tighter spacing for contact lines */
        }

        .contact-links p {
             margin-bottom: 0; /* No bottom margin needed here */
        }

        .contact-links a {
            margin: 0 var(--space-sm);
            font-weight: 600; /* Bolder links here */
            display: inline-block; /* Better spacing control */
            padding: var(--space-xs) 0; /* Add slight vertical padding */
            color: var(--primary-color); /* Primary color for main links */
        }
         .contact-links a:hover {
             color: var(--secondary-color); /* Swap colors on hover */
         }

        /* --- General Sections --- */
        .section {
            margin-bottom: var(--space-xl);
        }
        .section:last-of-type {
            margin-bottom: 0; /* No margin for the very last section */
        }

        /* --- Section Items (Education, Experience, Projects) --- */
        .section-item {
            margin-bottom: var(--space-lg);
            padding-left: var(--space-md);
            border-left: 4px solid var(--border-color);
            transition: border-color var(--transition-speed) ease;
        }
        .section-item:hover {
            border-left-color: var(--secondary-color); /* Accent color on hover */
        }
        .section-item:last-child {
             margin-bottom: 0;
        }

        .item-title {
            font-family: var(--font-headings);
            font-weight: 600;
            font-size: 1.2rem;
            color: var(--text-color);
            margin-bottom: var(--space-xs);
            display: block; /* Ensure it takes full width */
        }
        .item-title a { /* Styling for GitHub links within project titles */
            font-size: 0.9em; /* Slightly smaller */
            margin-left: var(--space-sm);
            font-weight: 500;
        }

        .item-subtitle {
            font-style: normal;
            font-weight: 600; /* Bolder subtitle */
            color: var(--text-muted);
            margin-bottom: var(--space-sm);
            font-size: 1rem;
        }

        .section-item p:not(.item-title):not(.item-subtitle) { /* Paragraphs within items */
            font-size: 0.98rem;
            color: var(--text-color);
            margin-bottom: var(--space-sm);
        }

        .section-item ul { /* Descriptive lists (job duties, courses) */
            list-style: disc;
            padding-left: var(--space-lg); /* Proper indentation */
            margin-top: var(--space-sm);
        }

        .section-item ul li {
            margin-bottom: var(--space-sm);
            line-height: 1.6;
        }

        /* --- Tag Lists (Skills, Technologies, Hobbies) --- */
        .tech-list, .hobby-list, .skill-list {
            list-style: none;
            padding-left: 0;
            margin-top: var(--space-sm); /* Add space above the list */
        }

        .tech-list li, .hobby-list li, .skill-list li {
            display: inline-block;
            background-color: var(--tag-bg);
            color: var(--tag-text);
            padding: var(--space-xs) var(--space-md); /* 4px 16px */
            margin: var(--space-xs) var(--space-sm) var(--space-xs) 0;
            border-radius: 15px; /* Pill shape */
            font-size: 0.9rem;
            font-weight: 500; /* Medium weight */
            border: 1px solid var(--tag-border);
            transition: background-color var(--transition-speed) ease, color var(--transition-speed) ease, transform var(--transition-speed) ease;
            cursor: default; /* Indicate non-interactive */
        }

        .tech-list li:hover, .hobby-list li:hover, .skill-list li:hover {
            background-color: var(--primary-color);
            color: var(--background-white);
            border-color: var(--primary-color);
            transform: translateY(-2px); /* Subtle lift effect */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        /* --- Specific Section Adjustments --- */
        /* Certifications */
        .certifications .section-item { /* Simpler style for certs */
             border-left: none;
             padding-left: 0;
        }
        .certifications .item-title {
             display: inline; /* Keep title inline */
             font-size: 1.1rem; /* Slightly smaller */
             margin-right: var(--space-sm);
             color: var(--text-color);
        }
         .certifications p {
            font-size: 1rem;
            color: var(--text-muted);
         }
         .certifications p .item-title { /* Ensure title color overrides muted */
             color: var(--text-color);
         }

        /* Languages */
        .languages .section-item {
            border-left: none;
            padding-left: 0;
            margin-bottom: var(--space-sm); /* Less space between languages */
        }
        .languages p {
             font-size: 1rem;
             margin-bottom: 0;
        }
        .languages strong { /* Language name */
            font-weight: 600;
            color: var(--text-color);
            margin-right: var(--space-sm);
        }

        /* --- Responsiveness --- */
        @media (max-width: 768px) {
            html {
                font-size: 15px; /* Slightly smaller base font on smaller screens */
            }
            .container {
                padding: var(--space-xl); /* Reduce padding */
            }
            h1 {
                font-size: 2.4rem;
            }
            h2 {
                font-size: 1.5rem;
            }
            .personal-info, .contact-links {
                padding-bottom: var(--space-md); /* Reduce padding */
                margin-bottom: var(--space-md);
            }
        }

        @media (max-width: 480px) {
             html {
                font-size: 14px;
             }
            body {
                padding: var(--space-md) 0; /* Reduce body padding */
            }
            .container {
                padding: var(--space-lg);
                border-top-width: 4px; /* Thinner top border */
            }
            h1 {
                font-size: 2rem;
            }
            h2 {
                font-size: 1.4rem;
            }
            .section-item {
                padding-left: var(--space-sm); /* Less indentation */
                border-left-width: 3px;
            }
             .section-item ul {
                 padding-left: var(--space-md);
             }
            .contact-links a {
                margin: 0 var(--space-xs); /* Less space between links */
                font-size: 0.95rem;
            }
            .tech-list li, .hobby-list li, .skill-list li {
                padding: 3px 10px; /* Smaller tags */
                font-size: 0.85rem;
            }
        }

        /* --- Print Styles --- */
        @media print {
            body {
                background-color: #fff;
                color: #000;
                padding: 0;
                font-size: 11pt; /* Adjust print font size */
                font-family: 'Times New Roman', Times, serif; /* Serif font often preferred for print */
            }
            .container {
                max-width: 100%;
                box-shadow: none;
                border: none;
                padding: 1cm; /* Use cm for print margins */
            }
            h1, h2, h3 {
                color: #000; /* Black headings */
                border-bottom-color: #ccc;
                page-break-after: avoid; /* Try to keep headings with content */
            }
            h1 { font-size: 20pt; }
            h2 { font-size: 14pt; }

            .personal-info, .contact-links, .section {
                border: none;
                margin-bottom: 1cm;
                padding-bottom: 0;
            }
            .section-item {
                 border-left: none;
                 padding-left: 0;
                 margin-bottom: 0.5cm;
                 page-break-inside: avoid; /* Prevent items from splitting across pages */
            }

            a {
                color: #000; /* Black links */
                text-decoration: none; /* Optional: remove underline */
            }
            /* Optional: Show link URLs in print */
            /* a[href^="http"]::after {
                content: " (" attr(href) ")";
                font-size: 9pt;
                color: #555;
            } */
             .contact-links a { margin: 0 5px; }

            .tech-list li, .hobby-list li, .skill-list li {
                background-color: #eee; /* Light gray background for tags */
                color: #000;
                border: 1px solid #ccc;
                padding: 2px 6px;
                font-size: 9pt;
                border-radius: 3px;
                transform: none;
                box-shadow: none;
            }

            /* Hide elements not needed for print if any */
            /* .no-print { display: none; } */
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>{{personalInfo.name}}</h1>
        <div class="personal-info section"> <p>{{personalInfo.email}} | {{personalInfo.phone}}</p>
            <p>{{personalInfo.address}}</p>
        </div>
        <div class="contact-links section"> <p>
                {{#if personalInfo.linkedin}}<a href="{{personalInfo.linkedin}}" target="_blank">LinkedIn</a>{{/if}}
                {{#if personalInfo.github}}<a href="{{personalInfo.github}}" target="_blank">GitHub</a>{{/if}}
                {{#if personalInfo.portfolio}}<a href="{{personalInfo.portfolio}}" target="_blank">Portfolio</a>{{/if}}
            </p>
        </div>

        {{#if education}}
        <div class="section education">
            <h2>Học vấn</h2>
            {{#each education}}
            <div class="section-item">
                <p class="item-title">{{degree}}</p>
                <p class="item-subtitle">{{school}} ({{year}})</p>
                {{#if gpa}}<p>GPA: {{gpa}}</p>{{/if}}
                {{#if relevantCourses}}
                <p>Các môn học liên quan:</p>
                <ul>
                    {{#each relevantCourses}}<li>{{this}}</li>{{/each}}
                </ul>
                {{/if}}
            </div>
            {{/each}}
        </div>
        {{/if}}

        {{#if experience}}
        <div class="section experience">
            <h2>Kinh nghiệm làm việc</h2>
            {{#each experience}}
            <div class="section-item">
                <p class="item-title">{{title}}</p>
                <p class="item-subtitle">{{company}} ({{duration}})</p>
                <p>{{description}}</p>
                {{#if technologies}}
                <p>Công nghệ sử dụng:</p>
                <ul class="tech-list">
                    {{#each technologies}}<li>{{this}}</li>{{/each}}
                </ul>
                {{/if}}
            </div>
            {{/each}}
        </div>
        {{/if}}

         {{#if projects}}
        <div class="section projects">
            <h2>Dự án cá nhân</h2>
            {{#each projects}}
            <div class="section-item">
                 <p class="item-title">{{name}} {{#if github}}(<a href="{{github}}" target="_blank">GitHub</a>){{/if}}</p>
                <p>{{description}}</p>
                {{#if technologies}}
                <p>Công nghệ sử dụng:</p>
                <ul class="tech-list">
                    {{#each technologies}}<li>{{this}}</li>{{/each}}
                </ul>
                {{/if}}
            </div>
            {{/each}}
        </div>
        {{/if}}

        {{#if skills}}
        <div class="section skills">
            <h2>Kỹ năng</h2>
            <ul class="skill-list">
                {{#each skills}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
        {{/if}}

        {{#if certifications}}
        <div class="section certifications">
            <h2>Chứng chỉ</h2>
            {{#each certifications}}
            <div class="section-item">
                 <p><span class="item-title">{{name}}</span> ({{year}}) - {{issuer}}</p>
            </div>
            {{/each}}
        </div>
        {{/if}}

        {{#if languages}}
        <div class="section languages">
            <h2>Ngôn ngữ</h2>
            {{#each languages}}
            <div class="section-item">
                <p><strong>{{language}}:</strong> {{level}}</p>
            </div>
            {{/each}}
        </div>
        {{/if}}

        {{#if hobbies}}
        <div class="section hobbies">
            <h2>Sở thích</h2>
            <ul class="hobby-list">
                {{#each hobbies}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
        {{/if}}

    </div> </body>
</html>